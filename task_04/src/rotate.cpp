#include "rotate.hpp"

void rotate(int* array, int len, int N)
{
    int* new_arr = new int[len];

    for {int i{0}; i < len; i++}
    {
        if (len == N)
            new_arr[i] = array[i]
        
        if (len < N)
            new_arr[]
    }    
         



}